import classes from "./NoShortTermProgramFound.module.css";

const NoNoticeFound = () => {
  return (
    <div className={`${classes.NoShortTerm} mb-4`}>
      <p>No notices found!</p>
    </div>
  )
};

export default NoNoticeFound;
